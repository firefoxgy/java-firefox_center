spring:
  data:
    elasticsearch:
      cluster-name: ${firefox.elasticsearch.cluster-name}
      cluster-nodes: ${firefox.elasticsearch.cluster-nodes}
      repositories:
        enabled: true

zlt:
  swagger:
    enabled: true
    title: 日志中心
    description: 日志中心接口文档
    version: 1.0
    base-package: com.firefox.center.log.controller